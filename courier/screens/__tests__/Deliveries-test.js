import * as React from 'react';
import {render} from '@testing-libray/react-native';

import DeliveriesScreen from '../DeliveriesScreen';

it('renders correctly', () => {
    render(<DeliveriesScreen />);
});
